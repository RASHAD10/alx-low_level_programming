#include "main.h"
#include <stdio.h>
/**
 * _abs - absolute value of an integer
 * @c: is the int of argument
 * Return: 0
 */
int _abs(int)
{
	if (c > 0 || c == 0)
	{
		return (c);
	}
	else
		return (c * -1);
}
